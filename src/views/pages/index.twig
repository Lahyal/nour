{% extends 'layouts/master' %}

{% block title %}{{ store.name }} — {{ store.tagline }}{% endblock %}

{% block content %}

  {# Hero Section #}
  {% component 'home.nour-hero' %}

  {# Trust Bar #}
  {% component 'home.nour-trust-bar' %}

  {# As Seen On #}
  {% component 'home.nour-seen-on' %}

  {# Salla native: Featured Products, Sliders, etc. #}
  {% component home %}

  {# Benefits Section #}
  {% component 'home.nour-benefits' %}

  {# Stats / Social Proof #}
  {% component 'home.nour-stats' %}

  {# Testimonials #}
  {% component 'home.nour-testimonials' %}

  {# Comparison Table #}
  {% component 'home.nour-compare' %}

  {# FAQ #}
  {% component 'home.nour-faq' %}

  {# Newsletter #}
  {% component 'home.nour-newsletter' %}

{% endblock %}
